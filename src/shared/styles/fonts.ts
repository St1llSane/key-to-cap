import { Chivo, Jost } from 'next/font/google'

export const jost = Jost({
  subsets: ['latin'],
  weight: ['400', '500'],
  style: ['normal'],
  display: 'swap'
})

export const chivo = Chivo({
  subsets: ['latin'],
  weight: ['700'],
  style: ['normal'],
  display: 'swap'
})
