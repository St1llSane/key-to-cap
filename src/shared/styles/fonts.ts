import { Inter, Jost } from 'next/font/google'

export const jost = Jost({
  subsets: ['latin'],
  weight: ['400', '500'],
  style: ['normal'],
  display: 'swap'
})

export const inter = Inter({
  subsets: ['latin'],
  weight: ['600', '700'],
  style: ['normal'],
  display: 'swap'
})
