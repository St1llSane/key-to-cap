import { Jost, Raleway } from 'next/font/google'

export const jost = Jost({
  subsets: ['latin'],
  weight: ['400', '500'],
  style: ['normal'],
  display: 'swap'
})

export const raleway = Raleway({
  subsets: ['latin'],
  weight: ['700'],
  style: ['normal'],
  display: 'swap'
})
